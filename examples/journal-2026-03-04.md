# 2026-03-04

A journal file as it actually looks during work: MARK anchors written in the moment, plus
whatever prose helps the next session catch up. The nightly sweep reads this file and every MARK
must produce a candidate card or an explicit "already carded" note — that is what makes the
anchors worth writing.

MARK[start] looking at the humidity gaps at the top of each hour (sess 8f21)
MARK[obs] gaps are humidity only; temperature from the SAME frames is complete (sess 8f21)
MARK[hyp] first guess was sensor brownout at hour boundaries — wrong, see below (sess 8f21)
MARK[obs] every missing reading is in the last minute before the hour (sess 8f21)
MARK[solved] humidity gaps were log rotation, not the sensor — writer kept the old handle (sess 8f21)

rotation triggered manually four times with a synthetic frame in flight; all four frames appear
in the correct file after the fix. Re-checked a week of production data: no gaps.

MARK[decision] summary job re-reads the rotated file after a delay longer than the flush interval,
rather than making rotation synchronous — synchronous rotation would block the writer (sess 8f21)
MARK[note] the "sensor brownout" hypothesis cost two hours; the counting check (frames received
vs frames stored) would have located it in five minutes (sess 8f21)

---

Day summary (appended by the nightly consolidate stage — factual, no recommendations):
Humidity gaps at hour boundaries were traced to log rotation racing the writer's buffer flush;
fixed by reopening by name and by having the summary job re-read the rotated file. Verified by
four manual rotations with a frame in flight and against a week of stored data. An earlier
sensor-brownout hypothesis was ruled out by the observation that temperature from the same
frames was complete.
