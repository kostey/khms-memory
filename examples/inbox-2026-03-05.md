# nightly sweep 2026-03-05 — candidates

What the pipeline produces: proposals with temporary labels, no ids, every specific backed by a
verbatim quote. A human (or a strong-model review stage) approves this file with
`approve_inbox.py`, which assigns the real ids and rewrites the cross-references — `C2`'s
`derived_from: [C1]` below becomes a real id pair.

## Cards

### C1: humidity gaps at hour boundaries were log rotation

```yaml
id: C1
type: problem→solution
level: observation
status: active
tags: [logging, gotcha]
scope: project:weather-station
evidence: measured
source: 'journal/2026-03-04.md lines 3-11'
date: 2026-03-04
links:
  derived_from: []
  supports: []
  contradicts: []
  supersedes: null
  refuted_by: []
---
SYMPTOM: one to three humidity readings missing at the top of most hours; temperature from the
same frames complete.
CAUSE: log rotation renamed the file out from under the writer, which held its handle open.
FIX: reopen by name after rotation; the summary job re-reads the rotated file after the flush
interval.
VERIFIED: four manual rotations with a frame in flight, all four frames in the correct file.

**QUOTES:**
- src=journal :: MARK[solved] humidity gaps were log rotation, not the sensor — writer kept the old handle
- src=journal :: rotation triggered manually four times with a synthetic frame in flight
```

### C2: count what was produced against what was stored, at each hop

```yaml
id: C2
type: principle
level: derived
status: active
tags: [debugging, data-quality]
scope: universal
date: 2026-03-04
links:
  derived_from: [C1]
  supports: []
  contradicts: []
  supersedes: null
  refuted_by: []
---
HOLDS: a dropped record leaves a series that still looks like a series, so missing data is found
by counting at each hop, not by inspecting the source.
LIMITS: one observation so far — this is a candidate pattern, not an established one.
IMPLICATIONS: count first; it is a measurement rather than an inference, and it is cheap.

**LINKS:** derived_from=[C1]

**QUOTES:**
- src=journal :: the counting check (frames received vs frames stored) would have located it in five minutes
```

## Flagged

- **C2 is thin.** One observation, presented as a pattern. Either hold it until a second
  independent case appears, or approve it as `level: assumption`. Reviewer's call — this is
  exactly the kind of decision the pipeline must not make on its own.
- The day's MARK[hyp] line (sensor brownout) produced no card. That is correct — a refuted
  first guess with no measurement behind it is noise — but it is recorded here so review can
  disagree.

## Dropped

- 3 candidates: restatements of C1 from different points in the transcript (deduplicated).
- 1 candidate: ungrounded — asserted a specific flush interval that appears in no source.
